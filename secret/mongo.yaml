apiVersion: apps/v1
kind: Deployment
metadata:
  name: ###
spec:
  selector:
    matchLabels:
      type: ###
  template:
    metadata:
      labels:
        type: ###
    spec:
      containers:
      - name: ###
        image: ###
        ports:
        - containerPort: ###
        envFrom:
          - secretRef:
              name: ###

